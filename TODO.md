# TODO

- [x] Fix web bundling crash caused by importing native-only `react-native-maps` from `app/_components/MapContainer.tsx`
- [ ] Re-run Web bundling (`npm run web`) and confirm the `web bundling failed` error is gone
- [ ] Fix any remaining runtime/build errors for web
- [ ] Start Android build (`npm run android`) and confirm it installs/compiles successfully
- [ ] Capture any remaining errors and patch accordingly

